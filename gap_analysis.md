# 合服预演规则清单

## 已实现能力

### 1. 多文件数据处理
- 支持上传多个 CSV。
- 支持上传一个 XLSX 计划表。

### 2. 逻辑组识别
- 支持单行一组。
- 支持多行共享成员并识别为同一逻辑组。
- 支持 `参与服` 中包含多个区服。

### 3. 合服重组
- 支持把指定输入对抽出为新的请求组。
- 支持将剩余成员自动合并为新的剩余组。
- 支持同组拆分和跨组抽组。

### 4. 风险预警
- 支持常规预警：
  - 排名接近
  - 高战高充
  - 战力接近
- 支持剩余组二次 DAU 预警：
  - 条件为 `DAU < 5`

### 5. 数据清洗
- 支持目标服唯一化。
- 支持参与服去重。
- 支持参与服英文逗号拼接。

### 6. 输出文件
- 输出 `alert_result.csv`
- 输出 `swapped_log.csv`
- 输出 `result_plan.xlsx`

## 页面展示

### 1. 统计卡片
- 常规预警组
- 二次预警组
- 成功重组组

### 2. 详情预览
- 支持预览警报组
- 支持预览二次预警组
- 支持预览重组记录

## 测试覆盖
- 已为以下规则补充单元测试：
  - 逻辑组解析
  - 指定区服对重组
  - 常规预警判定
  - 二次 DAU 预警判定
  - 最终目标服去重合并
