# 合服预演需求说明

## 输入

### 1. 服务器数据 CSV
- 支持上传多个 CSV 文件。
- 关键字段包括：
  - `区服ID`
  - `前3名战力之和`
  - `最高玩家累充金额`
  - `DAU`
- CSV 使用第二行作为表头，读取时会跳过第一行索引。

### 2. 合服计划 XLSX
- 关键字段包括：
  - `目标服`
  - `参与服`
- `参与服` 可以是单个区服，也可以是多个区服的英文逗号拼接。

### 3. 指定区服对
- 文本框输入，每行一组。
- 格式示例：

```text
409474,409370
409375,409314
```

## 处理规则

### 1. 数据预处理
- 合并所有上传的 CSV。
- 将关键字段转换为数值类型。
- 按 `前2名战力之和` 降序排序。
- 生成 `真实排名` 字段。

### 2. 逻辑组识别
- 系统会先解析 Excel 中的 `目标服` 和 `参与服`。
- 共享成员的多行记录会被识别为同一个逻辑组。

### 3. 重组规则
- 对于每个输入对 `(A, B)`：
  - 将 `A` 和 `B` 从原逻辑组中抽出，组成新的请求组。
  - 将原组剩余成员汇总为新的剩余组。
  - 剩余组中，区服 ID 最小者作为 `目标服`，其余作为 `参与服`。

### 4. 常规预警
- 仅对输入对 `(A, B)` 进行检测。
- 满足以下任一条件即触发预警：
  1. 排名接近：`abs(排名1 - 排名2) <= 5`
  2. 高战高充：双方均位于前 25%，且 `最高玩家累充金额 >= 5000`
  3. 战力接近：`abs(前2名战力之和1 - 前2名战力之和2) <= 5亿`

### 5. 二次 DAU 预警
- 仅在 `(A, B)` 已触发常规预警时执行。
- 检查重组后的剩余组。
- 若剩余组中存在任意区服满足 `DAU < 5`，则触发二次 DAU 预警。

### 6. 最终结果清洗
- 对最终结果中的 `目标服` 做唯一化处理。
- 若同一个 `目标服` 出现多次，则合并为一行。
- 对应的 `参与服` 去重后使用英文逗号拼接。
- `参与服` 按区服 ID 升序输出。

## 输出

### 1. `alert_result.csv`
- 输出所有常规预警和二次 DAU 预警涉及的区服详情。
- 包含：
  - `警报类型`
  - `警报组ID`
  - `警报原因`

### 2. `swapped_log.csv`
- 输出每次重组的记录。
- 包含：
  - 合并申请
  - 来源行号
  - 重组前组合
  - 重组后组合

### 3. `result_plan.xlsx`
- 输出最终重组后的合服计划表。
- 变更过的锚点行会标黄。

### 4. 网页统计
- 常规预警组数量
- 二次 DAU 预警组数量
- 成功重组组数量
